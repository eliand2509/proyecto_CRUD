<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Docente</title>
    <link rel="stylesheet" href="/MiproyectoMASP/public/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Editar Docente</h1>
        <form action="/MiproyectoMASP/public/docentes/actualizar" method="post" class="registro-box">
            <input type="hidden" name="id" value="{{ docente.id }}">

            <div class="input-group">
                <label for="cedula">Cédula:</label>
                <input type="text" id="cedula" name="cedula" value="{{ docente.cedula }}" required>
                {% if errores.cedula %}
                    <div class="error">{{ errores.cedula }}</div>
                {% endif %}
            </div>

            <div class="input-group">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" name="nombre" value="{{ docente.nombre }}" required>
                {% if errores.nombre %}
                    <div class="error">{{ errores.nombre }}</div>
                {% endif %}
            </div>

            <div class="input-group">
                <label for="apellido">Apellido:</label>
                <input type="text" id="apellido" name="apellido" value="{{ docente.apellido }}" required>
                {% if errores.apellido %}
                    <div class="error">{{ errores.apellido }}</div>
                {% endif %}
            </div>

            <div class="input-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" value="{{ docente.email }}" required>
                {% if errores.email %}
                    <div class="error">{{ errores.email }}</div>
                {% endif %}
            </div>

            <div class="input-group">
                <label for="telefono">Teléfono:</label>
                <input type="text" id="telefono" name="telefono" value="{{ docente.telefono }}" required>
                {% if errores.telefono %}
                    <div class="error">{{ errores.telefono }}</div>
                {% endif %}
            </div>

            <button type="submit">Guardar Cambios</button>
        </form>
        <p><a href="/MiproyectoMASP/public/docentes">Volver a la lista de docentes</a></p>
    </div>
</body>
</html>