<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Registro de Usuario</title>
    <link rel="stylesheet" href="/MiproyectoMASP/public/css/style.css">
</head>
<body>
    <div class="container">
        <h2>Registro de  Nuevo Usuario</h2>
        <form action="/MiproyectoMASP/public/registro" method="POST">
            <div class="form-group">
                <label for="nombre">Nombre Completo:</label>
                <input type="text" id="nombre" name="nombre" value="{{ old_data.nombre|default('') }}" required>
                {% if errores.nombre %}
                    <div class="error-message">{{ errores.nombre }}</div>
                {% endif %}
            </div>
            <div class="form-group">
                <label for="cedula">Cédula de identidad:</label>
                <input type="text" id="cedula" name="cedula" value="{{ old_data.cedula|default('') }}" required>
                {% if errores.cedula %}
                    <div class="error-message">{{ errores.cedula }}</div>
                {% endif %}
            </div>
            <div class="form-group">
                <label for="cargo">Cargo en el Departamento</label>
                <input type="text" id="cargo" name="cargo" value="{{ old_data.cargo|default('') }}" required>
                {% if errores.cargo %}
                    <div class="error-message">{{ errores.cargo }}</div>
                {% endif %}
            </div>
            <div class="form-group">
                <label for="rol">Rol:</label>
                <select id="rol" name="rol" required>
                    <option value=""> Seleccionar Rol</option>
                    <option value="administrador" {% if old_data.rol == 'administrador' %}selected{% endif %}>Administrador</option>
                    <option value="usuario" {% if old_data.rol == 'usuario' %}selected{% endif %}>Usuario</option>
                </select>
                {% if errores.rol %}
                    <div class="error-message">{{ errores.rol }}</div>
                {% endif %}
            </div>
            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" required>
                {% if errores.password %}
                    <div class="error-message">{{ errores.password }}</div>
                {% endif %}
            </div>
            <div class="form-group">
                <label for="confirm_password">Confirmar Contraseña:</label>
                <input type="password" id="confirm_password" name="confirm_password" required>
                {% if errores.confirm_password %}
                    <div class="error-message">{{ errores.confirm_password }}</div>
                {% endif %}
            </div>
            <button type="submit">Registrar Usuario</button>
            {% if error %}
                <div class="error-message">{{ error|raw }}</div>
            {% endif %}
        </form>
    </div>
</body>
</html>